---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-28T16:22:40.154Z
provider: 
---

# 前端页面验证规则

## 触发条件

当进行以下操作时，必须执行前端页面验证：
- 修改前端代码（`.tsx`、`.ts`、`.css`、`.html` 等文件）
- 修改后端 API 接口（可能影响前端数据展示）
- 部署前端代码到 COS/CDN
- 用户要求验证前端功能

## 验证流程

### 1. 启动本地开发服务器（如需要）
```bash
cd frontend && npm run dev
```

### 2. 使用 preview_url 工具打开页面
- 本地开发：`http://localhost:5173/#/页面路径`
- 生产环境：`https://ppt.bottlepeace.com/#/页面路径`

### 3. 验证清单

#### 页面加载验证
- [ ] 页面能正常加载，无白屏
- [ ] 无控制台报错（JavaScript 错误）
- [ ] 无网络请求失败（API 调用正常）
- [ ] 页面样式正确渲染

#### 功能交互验证
- [ ] 所有按钮可点击且功能正常
- [ ] 表单输入、验证、提交正常
- [ ] 数据列表加载和分页正常
- [ ] 弹窗/对话框正常显示和关闭
- [ ] 路由跳转正常

#### 数据流验证
- [ ] API 请求参数正确
- [ ] API 响应数据正确解析
- [ ] 数据正确展示在页面上
- [ ] 错误状态正确处理和提示

### 4. 验证方式

使用以下工具进行验证：
1. **preview_url**: 打开页面进行可视化检查
2. **curl/execute_command**: 直接测试 API 接口
3. **read_lints**: 检查代码 lint 错误

### 5. 验证标准

**必须满足以下所有条件才能完成验证：**
- ✅ 页面所有功能均已测试
- ✅ 无 JavaScript 控制台错误
- ✅ 无 API 请求失败
- ✅ 用户交互流程完整且顺畅
- ✅ 数据展示正确无误

## 注意事项

1. **不要跳过验证**：即使只是小改动，也必须验证相关功能
2. **完整验证**：验证所有相关页面和功能，不要只验证修改的部分
3. **记录问题**：发现问题立即修复，不要遗留
4. **重复验证**：修复后必须重新验证，直到完全通过
5. **生产验证**：部署到生产环境后，必须在生产 URL 再次验证

## 常见页面路径

| 页面 | 路径 | 主要功能 |
|------|------|----------|
| 大纲编辑 | `/#/outline` | 智能生成、向导生成、大纲编辑、保存 |
| PPT组装 | `/#/assembly/:id` | 章节管理、幻灯片预览、导出 |
| PPT生成 | `/#/generation` | 任务创建、进度查看、历史记录 |
| 文档管理 | `/#/documents` | 文档上传、列表、搜索 |
| 精调页面 | `/#/refinement/:id` | 页面编辑、AI对话、版本管理 |